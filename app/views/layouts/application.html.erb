<!DOCTYPE html>
<html data-theme="light">
  <head>
    <title>SistemaAcademico</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="bg-white text-black p-5 flex justify-between items-center md:p-7">
        <%= link_to root_path do%>
          <h1 class="text-3xl font-bold md:text-4xl transition-colors duration-300 hover:text-primary">Turmaria</h1>
        <% end %>
        <nav class="flex space-x-3.5 items-center md:space-x-7">
          <% if current_account.present? && current_page?(root_path) %>
            <%= link_to new_escola_path do %>
              Escolas   
            <% end %>
            <%= link_to new_turma_path do %>
              Turmas 
            <% end %>
            <%= link_to new_aluno_path do %>
              Alunos
            <% end %>
          <% end %>
          <% if current_account.present? %>
            <%= button_to destroy_user_session_path, method: :delete, class: "btn bg-red-600 text-white shadow-none d-flex align-items-center gap-2" do %>
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a2 2 0 002 2h3a2 2 0 002-2v-1m-7-8V7a2 2 0 012-2h3a2 2 0 012 2v1" />
              </svg>
              Sair
            <% end %>
          <% else %>
            <%= link_to new_user_session_path, class: "btn btn-primary shadow-none d-flex align-items-center gap-2" do %>
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 16l-4-4m0 0l4-4m-4 4h14m-6 4v1a2 2 0 002 2h3a2 2 0 002-2v-1m-7-8V7a2 2 0 012-2h3a2 2 0 012 2v1" />
              </svg>
              Entrar
            <% end %>
          <% end %>
        </nav>
    </header>
    <main>
      <% if notice.present? %>
        <p class="notice"><%= notice %></p>
      <% end %>
      <% if alert.present? %>
        <p class="alert"><%= alert %></p>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
