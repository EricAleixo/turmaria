<%= form_with(model: escola, class: "bg-white p-8 rounded-lg shadow-md") do |form| %>
  <% if escola.errors.any? %>
    <div class="alert alert-error mb-6">
      <div>
        <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current flex-shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
        <span>
          <h3 class="font-bold">Erro ao salvar!</h3>
          <ul class="list-disc list-inside">
            <% escola.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </span>
      </div>
    </div>
  <% end %>

  <div class="flex justify-between items-center mb-8 border-b pb-4">
    <h1 class="text-2xl font-bold text-gray-700"><%= escola.persisted? ? "Editar Escola" : "Nova Escola" %></h1>
  </div>

  <div class="form-control w-full mb-6">
    <%= form.label :nome, "Nome da Escola *", class: "label label-text text-gray-600 font-semibold" %>
    <%= form.text_field :nome, class: "input input-bordered w-full", placeholder: "Ex: Colégio Estadual Dom Pedro II", required: true %>
  </div>

  <div class="form-control w-full mb-6">
    <%= form.label :cnpj, "CNPJ *", class: "label label-text text-gray-600 font-semibold" %>
    <%= form.text_field :cnpj, class: "input input-bordered w-full", placeholder: "XX.XXX.XXX/XXXX-XX", required: true %>
    <label class="label">
      <span class="label-text-alt text-gray-500">Formato: XX.XXX.XXX/XXXX-XX</span>
    </label>
  </div>

  <!-- Campos do Endereço -->
<h3 class="text-xl font-semibold text-gray-600">Endereço</h3>
<%= form.fields_for :endereco do |e| %>
  <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
    <div class="form-control w-full mb-6">
      <%= e.label :logradouro, "Logradouro *", class: "label label-text text-gray-600 font-semibold" %>
      <%= e.text_field :logradouro, class: "input input-bordered w-full", placeholder: "Ex: Rua das Flores", required: true %>
    </div>

    <div class="form-control w-full mb-6">
      <%= e.label :numero, "Número *", class: "label label-text text-gray-600 font-semibold" %>
      <%= e.text_field :numero, class: "input input-bordered w-full", placeholder: "Ex: 123", required: true %>
    </div>

    <div class="form-control w-full mb-6">
      <%= e.label :bairro, "Bairro", class: "label label-text text-gray-600 font-semibold" %>
      <%= e.text_field :bairro, class: "input input-bordered w-full", placeholder: "Ex: Centro" %>
    </div>

    <div class="form-control w-full mb-6">
      <%= e.label :cidade, "Cidade", class: "label label-text text-gray-600 font-semibold" %>
      <%= e.text_field :cidade, class: "input input-bordered w-full", placeholder: "Ex: São Paulo" %>
    </div>

    <div class="form-control w-full mb-6">
      <%= e.label :estado, "Estado", class: "label label-text text-gray-600 font-semibold" %>
      <%= e.text_field :estado, class: "input input-bordered w-full", placeholder: "Ex: SP" %>
    </div>

    <div class="form-control w-full mb-6">
      <%= e.label :cep, "CEP", class: "label label-text text-gray-600 font-semibold" %>
      <%= e.text_field :cep, class: "input input-bordered w-full", placeholder: "Ex: 01234-567" %>
    </div>
  </div>
<% end %>


  <div class="flex justify-between mt-10">
    <%= link_to "Voltar", request.referer.present? ? request.referer : escola_path(escola), class: "btn btn-ghost" %>
    <%= form.submit escola.persisted? ? "Atualizar" : "Criar Escola", class: "btn bg-green-500 hover:bg-green-600 text-white" %>
  </div>

<% end %>